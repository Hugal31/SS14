local Self = require "datum.objective.changeling_team_objective.class"
return {
    CLASS = Self,
    impersonate_department = require "datum.objective.changeling_team_objective.impersonate_department.mod",
}
