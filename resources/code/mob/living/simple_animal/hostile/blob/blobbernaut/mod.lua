local Self = require "mob.living.simple_animal.hostile.blob.blobbernaut.class"
return {
    CLASS = Self,
    independent = require "mob.living.simple_animal.hostile.blob.blobbernaut.independent.mod",
}
