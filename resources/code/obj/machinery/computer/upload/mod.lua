local Self = require "obj.machinery.computer.upload.class"
return {
    CLASS = Self,
    borg = require "obj.machinery.computer.upload.borg.mod",
    ai = require "obj.machinery.computer.upload.ai.mod",
}
