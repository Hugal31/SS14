local Self = require "mob.living.silicon.robot.class"
return {
    CLASS = Self,
    shell = require "mob.living.silicon.robot.shell.mod",
    modules = require "mob.living.silicon.robot.modules.mod",
}
