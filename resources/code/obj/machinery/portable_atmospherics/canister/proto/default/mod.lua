local Self = require "obj.machinery.portable_atmospherics.canister.proto.default.class"
return {
    CLASS = Self,
    oxygen = require "obj.machinery.portable_atmospherics.canister.proto.default.oxygen.mod",
}
