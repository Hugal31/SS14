local Self = require "area.security.execution.class"
return {
    CLASS = Self,
    education = require "area.security.execution.education.mod",
    transfer = require "area.security.execution.transfer.mod",
}
