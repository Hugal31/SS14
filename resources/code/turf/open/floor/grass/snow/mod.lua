local Self = require "turf.open.floor.grass.snow.class"
return {
    CLASS = Self,
    safe = require "turf.open.floor.grass.snow.safe.mod",
    basalt = require "turf.open.floor.grass.snow.basalt.mod",
}
