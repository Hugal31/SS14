local Self = require "obj.effect.proc_holder.spell.targeted.projectile.dumbfire.class"
return {
    CLASS = Self,
    juggernaut = require "obj.effect.proc_holder.spell.targeted.projectile.dumbfire.juggernaut.mod",
}
