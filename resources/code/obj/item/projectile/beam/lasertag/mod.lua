local Self = require "obj.item.projectile.beam.lasertag.class"
return {
    CLASS = Self,
    bluetag = require "obj.item.projectile.beam.lasertag.bluetag.mod",
    redtag = require "obj.item.projectile.beam.lasertag.redtag.mod",
}
