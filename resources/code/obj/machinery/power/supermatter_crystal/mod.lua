local Self = require "obj.machinery.power.supermatter_crystal.class"
return {
    CLASS = Self,
    shard = require "obj.machinery.power.supermatter_crystal.shard.mod",
    engine = require "obj.machinery.power.supermatter_crystal.engine.mod",
}
