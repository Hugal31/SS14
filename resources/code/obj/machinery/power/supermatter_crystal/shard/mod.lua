local Self = require "obj.machinery.power.supermatter_crystal.shard.class"
return {
    CLASS = Self,
    hugbox = require "obj.machinery.power.supermatter_crystal.shard.hugbox.mod",
    engine = require "obj.machinery.power.supermatter_crystal.shard.engine.mod",
}
