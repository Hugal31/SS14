local Self = require "obj.structure.closet.lasertag.class"
return {
    CLASS = Self,
    blue = require "obj.structure.closet.lasertag.blue.mod",
    red = require "obj.structure.closet.lasertag.red.mod",
}
