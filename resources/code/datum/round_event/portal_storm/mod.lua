local Self = require "datum.round_event.portal_storm.class"
return {
    CLASS = Self,
    portal_storm_narsie = require "datum.round_event.portal_storm.portal_storm_narsie.mod",
    syndicate_shocktroop = require "datum.round_event.portal_storm.syndicate_shocktroop.mod",
}
