local Self = require "atom.movable.class"
return {
    CLASS = Self,
    lighting_object = require "atom.movable.lighting_object.mod",
    virtualspeaker = require "atom.movable.virtualspeaker.mod",
}
