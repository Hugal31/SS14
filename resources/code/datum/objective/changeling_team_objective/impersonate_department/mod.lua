local Self = require "datum.objective.changeling_team_objective.impersonate_department.class"
return {
    CLASS = Self,
    impersonate_heads = require "datum.objective.changeling_team_objective.impersonate_department.impersonate_heads.mod",
}
