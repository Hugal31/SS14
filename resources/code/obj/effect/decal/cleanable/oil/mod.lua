local Self = require "obj.effect.decal.cleanable.oil.class"
return {
    CLASS = Self,
    slippery = require "obj.effect.decal.cleanable.oil.slippery.mod",
    streak = require "obj.effect.decal.cleanable.oil.streak.mod",
}
