local Self = require "obj.effect.proc_holder.spell.aoe_turf.conjure.construct.class"
return {
    CLASS = Self,
    lesser = require "obj.effect.proc_holder.spell.aoe_turf.conjure.construct.lesser.mod",
}
