local Self = require "obj.machinery.light.small.class"
return {
    CLASS = Self,
    built = require "obj.machinery.light.small.built.mod",
    broken = require "obj.machinery.light.small.broken.mod",
}
