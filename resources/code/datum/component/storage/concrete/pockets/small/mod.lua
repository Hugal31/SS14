local Self = require "datum.component.storage.concrete.pockets.small.class"
return {
    CLASS = Self,
    helmet = require "datum.component.storage.concrete.pockets.small.helmet.mod",
    fedora = require "datum.component.storage.concrete.pockets.small.fedora.mod",
}
