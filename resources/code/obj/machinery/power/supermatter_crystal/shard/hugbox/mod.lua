local Self = require "obj.machinery.power.supermatter_crystal.shard.hugbox.class"
return {
    CLASS = Self,
    fakecrystal = require "obj.machinery.power.supermatter_crystal.shard.hugbox.fakecrystal.mod",
}
