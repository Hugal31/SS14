local Self = require "datum.component.storage.concrete.pockets.small.fedora.class"
return {
    CLASS = Self,
    detective = require "datum.component.storage.concrete.pockets.small.fedora.detective.mod",
}
