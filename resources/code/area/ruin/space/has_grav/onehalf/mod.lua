local Self = require "area.ruin.space.has_grav.onehalf.class"
return {
    CLASS = Self,
    bridge = require "area.ruin.space.has_grav.onehalf.bridge.mod",
    dorms_med = require "area.ruin.space.has_grav.onehalf.dorms_med.mod",
    drone_bay = require "area.ruin.space.has_grav.onehalf.drone_bay.mod",
    hallway = require "area.ruin.space.has_grav.onehalf.hallway.mod",
}
