local Self = require "obj.structure.fluff.bus.class"
return {
    CLASS = Self,
    passable = require "obj.structure.fluff.bus.passable.mod",
    dense = require "obj.structure.fluff.bus.dense.mod",
}
